# ADAM - Annotation et Données Automatisées

git push -u origin feature/fbi-caller `       
  -o merge_request.create `       
  -o merge_request.target=feature/filtre-organisation `       
  -o merge_request.title="feat(auth): raccorde l'API a FBI via exa-pie" `       
  -o merge_request.remove_source_branch       

# ADAM - Annotation et Données Automatisées


git push -u origin feature/ocr-connector-mistral -o merge_request.create -o merge_request.target=main -o merge_request.title="feat(worker): connecteur OCR Mistral"

# ADAM - Annotation et Données Automatisées


git push -u origin E1/ci-hotfix -o merge_request.create -o merge_request.target=develop -o merge_request.title="fix(ci): valeurs d'environnement de repli pour les tests"
